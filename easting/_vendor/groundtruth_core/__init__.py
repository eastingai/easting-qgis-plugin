"""GroundTruth engine: stdlib-only so it vendors into the QGIS plugin."""

# Stamped into every hosted extraction response so a result can be traced back
# to the engine that produced it.
__version__ = "0.5.0"
